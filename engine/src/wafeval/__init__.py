"""wafeval — WAF Evasion Lab mutation + testing engine.

Phase 3 scope: payload loader, mutator registry (one concrete mutator — lexical),
async runner, verdict classifier, raw JSON emitter. Analyzer, reporter, and
dashboard land in subsequent phases.
"""

__version__ = "0.3.0"
